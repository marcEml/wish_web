{% block stylesheets %}
<style>
nav {
    width: 100%;
    display: flex;
    margin: 0 auto;
    padding: 12px 1em;
    max-width: 100svw;
    justify-content: space-between;
}
    
.logo {
    gap: 8px;
    height: 44px;
    display: flex;
    align-items: center;
}

.logo a:hover {
    text-decoration: none;
}
    
.logo img {
    height: 44px;
}
    
.text-wrapper {
    font-weight: bold;
}
    
.auth {
    gap: 8px;
    display: flex;
    height: 44px;
}
</style>
{% endblock %}

<nav>
    <a class="logo" href="{{ path('app_home') }}">
        <img class="image" src="assets/img/image-22.png" />
        <div class="text-wrapper">Wish</div>
    </div>

    <div class="auth">
        <a href="{{ path('app_authentication_logout') }}" class="btn btn-danger">Déconnexion</a>
    </div>
</nav>